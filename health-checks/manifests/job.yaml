apiVersion: batch/v1
kind: Job
metadata:
  name: oneshot
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: work
          image: busybox:1.36
          command: ["sh", "-c", "sleep 20; exit 0"]